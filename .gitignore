/.forge/
.envrc
/tmp/
/build/

# go.mod and go.sum are committed - the default - so the module runs with a
# bare go run, and sync keeps them fresh. They are deliberately NOT ignored:
# a tracked file gitignore also names never converges, because every sync
# dirties the tree and the revision goes -dirty forever.
